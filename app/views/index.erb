<div class="wrapper">
  <div class="not-left"> 

  <% user = User.find(session[:user_id]) if session[:user_id] %>

  <%= CssHelper.authentication(user) %>

  <!-- write a helper method to replace display instance variable -->
  <%= erb @display.to_sym %>
  
  </div>

  <% links = [{display: "Sign Up", link: "/register"},
              {display: "2", link: "/"},
              {display: "3", link: "/"}] %>

  <%= CssHelper.nav_bar("Guess Muthafucka!", links) %>
</div>










